SET(qpf_SOURCES controller.cpp)
SET(qpf_HEADERS controller.hpp)
INCLUDE(${QT_USE_FILE})
ADD_DEFINITIONS(${QT_DEFINITIONS})

QT4_WRAP_CPP(qpf_HEADERS_MOC ${qpf_HEADERS})

ADD_LIBRARY(qpf ${qpf_SOURCES} ${qpf_HEADERS_MOC})
TARGET_LINK_LIBRARIES(qpf ${QT_LIBRARIES})
